import { createMuiTheme } from "@material-ui/core";

export const theme = createMuiTheme({  //Import from ./theme


    typography: {
        h1: {
            fontSize: "3rem",
        },
        h2: {
            fontSize: "2rem",
        }
    }
});